--- 
- name: Установка стандартных пакетов
  hosts: balancer
  roles:
    - role: default_packages
    - role: balancer
  tags:
    - install_packages

- name: Установка и настройка nginx c кастомной страницей
  hosts: webservers # Будет работать на узлах в группе nginx
  roles:
    - role: default_packages
    - role: nginx_custom
  tags:
    - nginx_custom
